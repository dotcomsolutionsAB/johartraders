<?php

$features = '';

$pos = $header_array['Features 1'];
$text = $data[$pos];
if($text != '')
{
    $features = $features.'<li>'.$text.'</li>';
}

$pos = $header_array['Features 2'];
$text = $data[$pos];
if($text != '')
{
    $features = $features.'<li>'.$text.'</li>';
}

$pos = $header_array['Features 3'];
$text = $data[$pos];
if($text != '')
{
    $features = $features.'<li>'.$text.'</li>';
}

$pos = $header_array['Features 4'];
$text = $data[$pos];
if($text != '')
{
    $features = $features.'<li>'.$text.'</li>';
}

$pos = $header_array['Features 5'];
$text = $data[$pos];
if($text != '')
{
    $features = $features.'<li>'.$text.'</li>';
}

$pos = $header_array['Features 6'];
$text = $data[$pos];
if($text != '')
{
    $features = $features.'<li>'.$text.'</li>';
}

$pos = $header_array['Features 7'];
$text = $data[$pos];
if($text != '')
{
    $features = $features.'<li>'.$text.'</li>';
}

$pos = $header_array['Features 8'];
$text = $data[$pos];
if($text != '')
{
    $features = $features.'<li>'.$text.'</li>';
}

$pos = $header_array['Features 9'];
$text = $data[$pos];
if($text != '')
{
    $features = $features.'<li>'.$text.'</li>';
}

$pos = $header_array['Features 10'];
$text = $data[$pos];
if($text != '')
{
    $features = $features.'<li>'.$text.'</li>';
}

?>